# User material inbox

Place user-supplied evidence here only when needed for a review. All other
contents of this directory are ignored by Git.

Record a hash, source description, retrieval locator, distribution class, and
review relationship in the corresponding evidence ledger. Do not quote or copy
private material into tracked reports unless the user explicitly approves the
specific excerpt.
